# --- Core web framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1

# --- HTTP client for reverse-proxying requests to backend services ---
httpx==0.27.0

# --- Redis client (async) for rate limiting + circuit breaker state ---
redis==5.0.4

# --- JWT auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- Settings management (env vars, .env parsing) ---
pydantic==2.7.1
pydantic-settings==2.2.1

# --- Prometheus metrics instrumentation ---
prometheus-client==0.20.0
prometheus-fastapi-instrumentator==7.0.0

# --- Testing ---
pytest==8.2.0
pytest-asyncio==0.23.6

# --- Load testing script dependency ---
aiohttp==3.9.5